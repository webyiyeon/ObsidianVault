# 디자인 컴포넌트
- `icon_type` 속성은 잘 모르겠으면 `btn_check`로 통일
- `<SearchSection>` 쪽 컴포넌트 `btn_init`(초기화) 추가
- 디자인 컴포넌트는 모두 Full HD 기준 (width/height 작성 시 염두)
- 스크롤은 각 컴포넌트 별로 설정
	- 변경관리, 작업허가서 같이 컴포넌트가 너무 많은 건 어쩔 수 없이 전체 스크롤 사용 가능
- 문서 전체 저장/미리보기 등은 공통 영역(confirm 버튼 위치)에 적용
	- `additionalBtns` 옵션에서 사용하는데, setTitle 기능 점검 
- Mail Component, File Component 디자인 반영 필요
- Grid 편집 모드에서 넘어갈 때 tab으로 넘어가냐? onEnter 추가?
- 상세보기 화면 들어갈 때 `행 클릭 + 버튼` 기능과 `더블클릭` 둘 다 활성화
- 우측 상단 My favorite, My alarm 버튼 삭제
- 