
## SKMU

- [ ] IV(초기화 벡터) 하드코딩
      → 호출 옵션 속성 `false`로 변경. 해당 코드 삭제(HTTPS 사용)
- [ ] Client side AES 암호화 키 하드코딩
      → `raonkupload` 모듈 삭제
- [ ] 내부 테스트 및 관리용 API 경로 노출
      → url 모아둔 js 파일 내용을 각 화면으로 이동

## USHE

- [ ] IV(초기화 벡터) 하드코딩
      → 호출 옵션 속성 `false`로 변경. 해당 코드 삭제(HTTPS 사용)
- [x] 권한 체크 미흡으로 인해 판교 본사, 울산 등 전체 파일 노출
- [x] 암호화 또는 DRM 미적용으로 인한 기밀 정보 대량 유출 문제
      → 협력사의 경우 파일 경로를 통해 파일 찾아오는 대신,
      column 공통인 경우에는 임직원 데이터이더라도 FILE_CONTENT 로 호출하여 사용
- [x] Azure AD 자격증명 평문 노출
      → 파일 삭제
- [x] 전체 DB 테이블 구조 및 컬럼 정보 무단 열람 가능
      → 파일 삭제 (ushe.html)
- [x] 개인정보 유출 우려
      → 파일 삭제
- [ ] Client side AES 암호화 키 하드코딩
      → `raonkupload` 모듈 삭제
- [ ] Brute Force Attack + AES 암/복호화
      → `.env` 설정 변경
- [x] 로그인 페이지 SQL 쿼리 노출
      → 에러 로그 그대로 노출에 대한 예외 처리
- [x] 전체 게시글 조회 가능
      → `partner_yn` 파라미터 조작으로 조회 가능
- [x] 전체 협력업체 정보 조회 및 협력업체로 로그인 가능
      → `partner_yn` 파라미터 조작으로 조회 가능
- [ ] CCTV 정보 노출 (설치 위치, IP 주소, 포트, 패스워드)
      → 박수빈M이 마킹 후 재업로드 예정
- [x] 웹 서버 정보 노출 (Server Header Disclosure)
      → httpd conf 파일에 파라미터 추가

# 개발 서버 백엔드 종료 이슈 (2026-04-03)

사유는 알 수 없으나 `2026-04-03 20:00:16`에 `stopping she-back.service` 된 내역이 있음

![[이미지 (1).jpg|100]]
![[이미지 (9).png|100]]
